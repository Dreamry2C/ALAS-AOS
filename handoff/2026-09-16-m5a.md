# Handoff 2026-09-16 · 阶段五开锣（-3 断网容灾 / -4 桥截图定档 双绿）

> 新建对话或上下文压缩后：先读本文与 `docs/roadmap-v3.md`，再从「下一步」继续。
> 上一篇：`2026-09-16-m4d.md`（M4-d 死账清仓 + 演示前置体检）。

## 当前状态

- **阶段四**：M4-a/b/c/d 全落地，git 最新 = `fd09337` 已 push。**DoD 只剩用户在场演示「开始挂机」**（前置体检全绿：游戏在装/guest 配置一致/VD+桥+wrapper 通）。
- **阶段五-3 断网/弱网容灾 ✅**（三层证据）：
  - 审计：脚本三态协议 + AlasUpdater 永不抛全折叠 SKIPPED + 启动链失败续走，无缺陷；锁清理位置实测确认（UNCHANGED 路径不清锁无害，fetch 前必清）。
  - PC 七场景（`.tmp/m5-netresil`）：UNCHANGED 479ms / UPDATED 1051ms / 首跑 DEPTH=1 1009ms / 拒连 2.4s / 黑洞 30.8s / 坏 DNS 1s 全 FAILED 正确降级 / fetch 路径清锁。
  - 真机 E2E：坏 URL 冷启 9s FAILED ls-remote → SKIPPED → 环境照起；恢复后 UNCHANGED 快进零下载（state mtime 佐证）。03:46 还有一次真实网络抖动留下的 FAILED fetch 自然降级。
- **阶段五-4 桥截图定档 ✅**：shell 域 nc 打 127.0.0.1:22300，30 轮 screencap（2.76MB 裸帧）**p50=30ms / p95=42ms / max=45ms**（stat 轮询地板 ~28ms，真值更小）。对照 ALAS >1s 线富余 33 倍，m0 兜底 0.109s 更快。HONOR PPG-AN00 定档远超需求。
- 设备现状：app（fd09337 同码 562eca1 构建号 18）在跑，proot RUNNING，guest 远程 URL 已恢复 git.lyoko.io，热更新健康。
- **adb 现挂两台设备**：本链 HONOR=`192.168.50.190:5555`，另有一台 `127.0.0.1:16385`（SM_S9080，非本链，别碰）。**今后 adb 一律 `-s 192.168.50.190:5555`**（debug.md 已入条目）。

## 下一步

1. **用户演示「开始挂机」端到端**（阶段四 DoD 收官项，必须用户在场）。演示 checklist：
   - 前置：游戏在 VD 前台（不在就先让 ALAS 拉起或手动确认 VD 画面）；悬浮球 → 面板。
   - 按「开始挂机」→ 状态行变「运行中·pid」、日志板滚动 ALAS 输出。
   - 观察游戏被实际操作（截图循环 + 点击生效），WebUI 控制台状态同步。
   - 按「停止挂机」→ runner 归零、环境（gui/VD/桥）仍在。
   - 面板外划走 App 再回来：会话无损（FGS 保活）。
2. 阶段五剩余：-1 多 ROM 实测（矩阵骨架 `docs/rom-matrix.md` 已立；候选第二台 GT Pro / 曾现身 adb 的 SM_S9080 待用户确认；官方版冲突演练一并）｜-2 长稳 2~3h + 息屏（依赖演示 + 息屏授权）｜-5 shizuku-m 产品化｜-6 Release + 三仓公开（**README 初稿已立**，占位项=机型清单/仓库地址/shizuku-m 获取方式）。
3. 随时可穿插的待用户项：真机重启验证（阶段三 DoD ③）；油数验收（游戏点到出击菜单页）；appId 身份（MaaFwApp→MaaAL 命名）。

## 关键路径与命令

- adb：**必须** `adb -s 192.168.50.190:5555`；远端复合命令整串再套一层双引号（Git Bash 剥一层引号的坑，debug.md 有条目）。
- app 文件日志：`/sdcard/Android/data/com.aliothmoon.maafw/files/log/app.log`——**shell 域直读**（run-as 碰 /sdcard 外部私有目录 Permission denied）；**FileLogTree 只记 WARN+**，INFO 级事件（如热更新 UNCHANGED）看 state 文件 mtime 或 logcat（tag=类名，HONOR 日志很吵缓冲几分钟就冲掉）。
- guest 配置核查：`adb -s ... shell "run-as com.aliothmoon.maafw cat files/rootfs/opt/alas/config/alas.json"`。
- 验证口：`adb -s ... forward tcp:22400 tcp:22400`（+22267）→ curl `/status`、`/`。
- 桥压测脚本：`.tmp/bench_bridge.sh`（gitignored；push 到 /data/local/tmp 跑，shell 域 nc——run-as 域禁 socket）。

## 关键决策与注意

- VD 分辨率恒 1280×720 不做用户可选项（桥协议钉死）；DataStore 孤儿键不清；guest 时间戳=UTC。
- 真机纪律不变：不私按「开始挂机」/不代点游戏/不私自锁屏息屏重启/tap 前核坐标；git 全预授权（release/tag 逐次）。
- 阶段五-1/-2 没动前，多 ROM 与长稳的任何「实测」都先核 roadmap §7 与风险登记。
